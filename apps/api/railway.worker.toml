# Copy these settings when creating the sync worker as a second Railway service.
# Root directory: apps/api
# Do not attach a public domain.

[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m app.workers.sync_worker --interval 5"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
